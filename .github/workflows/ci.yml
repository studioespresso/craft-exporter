name: ci
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - develop
permissions:
  contents: read
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  ci:
    name: ci
    uses: studioespresso/.github/.github/workflows/ci.yml@main
    with:
      craft_version: '5'
      php_version: "8.3"
      jobs: '["ecs", "phpstan", "changelog"]'
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
