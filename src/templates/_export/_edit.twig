{% extends "_layouts/cp" %}
{% import "_includes/forms" as forms %}

{% set titles = {
    1 : export ? export.name ~ ' - Element settings' : 'New export',
    2 : export.name ~ ' - Field settings',
    3 : export.name ~ ' - Export settings',
    4 : export.name ~ ' - Run export?',
} %}

{% set title = attribute(titles, step) %}
{% set crumbs = [
    { label: "Exporter", url: url('exporter') },
] %}

{% block content %}

        {{  sprig('exporter/sprig/element/step_' ~ step, {
            'elementTypeOptions': elementTypeOptions,
            'exportId': export ? export.id : null,
            's-trigger': 'load, refresh',
        }) }}


{% endblock %}